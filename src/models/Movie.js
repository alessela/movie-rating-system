class Movie {
    constructor(id, title, poster_path, release_date, genre_ids,)
    {
        this.id = id
        this.title = title
        this.poster_path = poster_path
        this.release_date = release_date
        this.genre_ids = genre_ids
    }
}

export default Movie;